# Copy to .env and fill in. Never commit .env.
# A GitHub personal access token (classic or fine-grained) with public_repo / read access.
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=PAT_TOKEN_HERE
# Optional overrides
REPO_OWNER=PostHog
REPO_NAME=posthog
LOOKBACK_DAYS=90
